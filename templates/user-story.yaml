title: <Story Title>
story_id: US-###
feature_id: F-##
flow_ids: []                 # Which flows this story participates in (e.g. ["authentication-flow"])
template: ~/.claude/file-templates/init-project/user-stories/story-title.md
status: incomplete
user_story:
  as_a: "[type of user]"
  i_want: "[goal]"
  so_that: "[benefit]"
acceptance_criteria:
  - "[ ] Given [context], when [action], then [outcome]"
  - "[ ] "
  - "[ ] "
context:
  why: ""
  related: ""
technical_notes:
  apis: []                   # List of API endpoints (e.g. ["POST /auth/login"])
  data_structures: []        # List of data models used
  data_events: []            # Events emitted by this story (e.g. ["user_login_attempt", "user_login_success"])
  feature_flags: []          # Any feature flags gating this story
  dependencies: []           # Dependencies and their purpose